# Diverse City FC Decision Log

Last updated: 2026-08-11

Statuses: `accepted`, `open`, `superseded`.

## Accepted Decisions

| ID | Decision | Rationale |
| --- | --- | --- |
| DCFC-D001 | Diverse City will become an Onzio tenant; the snapshot remains the visual specification. | Prevents a second production application and preserves shared tenant controls. |
| DCFC-D002 | Do not activate the snapshot's inherited Rose City backend. | It is incomplete, partially hardcoded, and disconnected from much of the approved public experience. |
| DCFC-D003 | Registration, payments, waivers, signatures, and participant records remain third-party responsibilities. | Keeps sensitive operational data and payment handling outside Onzio. |
| DCFC-D004 | Onzio may store approved external registration URLs as public content. | Supports branded Tryouts and program CTAs without collecting registrations. |
| DCFC-D005 | Phase 0-3 work is local-only and does not authorize hosted mutations. | Preserves the staging and production approval gates. |
| DCFC-D006 | Contact and Tryouts must be added to the approved visual specification before platform implementation is locked. | These pages do not exist in the pinned snapshot baseline. |
| DCFC-D007 | Club owners/admins edit normalized content and media; Onzio operators control presentation configuration. | Preserves the platform's content/presentation boundary. |
| DCFC-D008 | Production content cannot be backed by preview placeholders or unresolved claims. | Preserves factual and provenance requirements. |
| DCFC-D009 | Every assigned agent must update the shared status ledger before ending work. | Keeps progress recoverable across Codex, Claude Code, and future agents. |
| DCFC-D101 | Contact page copy, contact destinations, service area, and navigation placement approved: email/phone/Instagram/Facebook/X from `lib/site-data.ts`, location "Schaumburg, Illinois" only (no region suffix), original hero headline/intro copy, footer-only nav placement (Sponsors precedent). No hours were added. | Christian approved the local `/contact` implementation on 2026-07-31 after requesting removal of the "— Chicago Area" suffix from the location line. |
| DCFC-D104 | The approved Diverse City visual does not map to `cinematic@1`. It becomes its own new neutral reusable template, registered as `academy@1`, following the same extraction precedent as `clubhouse@1` (Lions). | Christian approved on 2026-07-31. `PLATFORM-GAP-ANALYSIS.md` found `cinematic@1` registers only `cinematic.hero`/`cinematic.gallery` (plus the shared `shared.next-match`/`shared.history`) — missing shop-feature, sponsor-carousel, standings, and programs-pathway sections Diverse City needs regardless of template choice — and no existing font pack matches Diverse City's Montserrat/Inter/DM Sans stack. Programs' two layout variants (statement-band vs. detail-image-plus-Program-Focus) are also a genuinely new section-composition shape. Reusing `cinematic@1` would mostly mean bolting Diverse-City-only sections onto Rose City's template rather than saving real work, so a clean new template was the better fit. Per `AGENTS.md`'s Documentation Rules, this does not require updating `docs/onzio-platform-plan.md` — the same precedent was followed when `clubhouse@1` was added without a plan-doc edit, since adding one more template is using the presentation system as designed, not a new architecture pattern. |
| DCFC-D105 | General tenant video is an approved capability, built as real self-hosted video (not a YouTube-embed) via a third-party video CDN — Bunny.net Stream — rather than in-house transcoding on Vercel/Supabase. Format/duration/dimension/size/poster/processing rules are not finalized by this decision; those are `DCFC-201`/`DCFC-202` implementation detail. | Christian approved on 2026-07-31 after a cost/build comparison. Rose City's only existing video ("legacy" `club.slug === "rose-city"` branch in `components/Hero.tsx`, a hardcoded Supabase Storage URL) is not a reusable capability, so this is genuinely new platform work either way. In-house transcoding was rejected: Vercel's serverless functions are a poor fit for video encoding (execution-time/memory limits, the same class of problem that previously forced `sharp`/`libvips` out of the media-cleanup runtime), and raw storage bandwidth (Supabase Storage egress $0.09/GB uncached, Vercel Blob $0.05/GB) both cost more per GB than a purpose-built video CDN at any realistic scale. Modeled cost at 10/20/50/100 clubs (2 short clips/club, ~500 monthly visits/club): Bunny.net Stream was cheapest at every scale (~$1.25 at 10 clubs, ~$12.50 at 100 clubs), with free standard H.264 encoding and a flat $1/mo minimum. Known tradeoffs accepted: less infrastructure control and weaker built-in analytics than Mux, and a smaller network than Cloudflare — judged acceptable for simple looping background/story videos with admin swap-in-out, not live streaming or DRM. Christian signed up for a Bunny.net account (with $20 starting credit) separately from this session. |
| DCFC-D108 | Tier gating for the three new domains: **Contact is Starter-accessible**; **Programs is Pro-only**; **Tryouts is Pro-only**. Implemented by adding `'contact'` to the `onzio_private.club_has_feature` Starter allowlist, while `programs` and `tryouts` use new feature strings that are Pro-only by default. | Christian confirmed on 2026-07-31. Raised during the `DCFC-103` re-review after discovering `club_has_feature` hardcodes the Starter allowlist `('branding', 'roster', 'schedule', 'homepage', 'about')` and that `can_read_feature` gates anonymous public reads — so an unexamined feature name would have silently made Starter clubs' contact pages render empty in public. Pro-only for Tryouts matches the already-registered `moduleRegistry.tryouts` → `entitlement: "pro"`. Adding `'contact'` to the allowlist was chosen over reusing the existing Starter-allowed `about` feature: reusing `about` carries no security downside (features are tier gates, not permission scopes) but would leave `ADMIN_TABLE_FEATURES` mapping contact tables to `"about"`, which is misleading to future readers. The allowlist change is purely additive and cannot affect existing tables. See `DOMAIN-DESIGN.md` for the implementation notes and the required `DCFC-201` contract coverage. |
| DCFC-D109 | The normalized domain design in `DOMAIN-DESIGN.md` (`onzio.programs`, `onzio.contact_profile`, `onzio.contact_page_content`, `onzio.tryouts`) is approved as the `DCFC-202` implementation target, including an explicit column-constraint policy. This closes `DCFC-103` and Phase 1. | Christian approved on 2026-07-31 after a verification pass that re-checked the design's load-bearing claims against real migration source rather than the draft's assertions: the `club_has_feature` Starter allowlist, the pre-existing `onzio.site_social_links` table, `media_assets.surface` being regex-constrained free-form rather than an enum, the absence of any existing email/phone column, and the `homepage_hero_content` singleton shape all confirmed. Christian additionally asked that column-length and URL-shape constraints be specified in the design rather than left for `DCFC-202` to decide while writing SQL. The added policy distinguishes `between 1 and N` (required, no default) from `<= N` (`not null default ''`, where empty is a legitimate unset value), and explicitly rejects reusing `homepage_hero_content`'s internal-path-only href regex for `programs.external_cta_href`/`tryouts.registration_href` — that pattern would reject every external registration URL those columns exist to hold. External-capable href columns instead mirror the `''`/local-path/`http:`/`https:`/`mailto:` allowlist already enforced in `lib/admin-data-contract.ts`. Approval does not authorize writing or applying the migration. |
| DCFC-D103 | Tryouts supports multiple structured event rows, not a single-opportunity singleton. Each row gets its own status, date, location, cost, and registration link, similar in shape to `matches`/`players` (a real table with an admin list/create/edit/reorder flow), not a single-form singleton like `homepage_hero_content`. | Christian confirmed on 2026-07-31 that running simultaneous tryout opportunities across the club's four distinct programs (Youth Academy, Special Kickers, Special Olympics, UPSL Men's Teams) — each with different eligibility and plausibly different windows — is a real, near-term scenario, not a hypothetical. Building the singleton model DCFC-002 shipped and later migrating to structured rows would require a real schema migration; building structured rows now avoids that. Per-row association to a specific program (e.g. an optional `program_id` reference) is left as `DCFC-202` schema detail, not decided here. |
| DCFC-D110 | Resolve the `academy@1` / `bebas-inter` font-pack compatibility inconsistency by **adding `"academy@1"` to `fontPacks["bebas-inter"].compatibleTemplates`**, treating `bebas-inter` as the universal fallback pack every template supports — rather than removing `bebas-inter` from `academy@1.compatibleFontPacks`. `DCFC-203` should also add a contract asserting bidirectional font-pack agreement so this class of drift cannot recur. | Christian approved on 2026-08-01. A `DCFC-203` audit found `academy@1.compatibleFontPacks` includes `bebas-inter` while `fontPacks["bebas-inter"].compatibleTemplates` omits `academy@1` — the only such disagreement among the four templates, so it was introduced with `academy@1`. It is reachable, not theoretical: `switchTemplate` keeps the font pack by reading the template's list (`packages/presentation/index.ts:839`) and validation then rejects the document by reading the font pack's list (`packages/presentation/index.ts:701`), so switching a `clubhouse@1` or `heritage@1` club onto `academy@1` emits a document that fails its own validation. The universal-fallback reading matches how `bebas-inter` is already registered for the other three templates. The rejected alternative was defensible — `DCFC-D104` found no existing pack matched Diverse City's Montserrat/Inter/DM Sans stack — but it would have made `academy@1` the only template with no fallback pack. |
| DCFC-D111 | Approve `DCFC-EPIC-002` and its per-package Class 3 approval model, authorizing the Phase 4-only goal for `DCFC-401` through `DCFC-404`. This approval authorizes Class 1 and loopback-only Class 2 work in Phase 4; it authorizes no hosted action, commit, push, or `DCFC-501` work. | Christian approved starting the recommended unbudgeted Phase 4 goal on 2026-08-01. |
| DCFC-D102 | For this rollout, publish no Tryouts event rows and no temporary Google registration link. Keep the reusable unavailable/contact state until the club enters real logistics and an approved HTTPS destination through admin. | Christian approved the recommended production baseline and confirmed current facts on 2026-08-01. This resolves the production-import disposition without inventing dates, eligibility, location, cost, or registration facts. |
| DCFC-D106 | Import no placeholder roster, staff, fixture, standings, or sponsor-opportunity rows. Retain the hardcoded Academy routes only with deliberate tenant-safe empty behavior; import the approved About, four Programs, Contact destinations, Elsa's Bakery, and contact-to-order Shop content. Authorize the smallest reusable empty/fallback corrections inside `DCFC-403`. | Christian approved the recommended baseline and confirmed current facts/rights on 2026-08-01. No club-slug special case is authorized. |
| DCFC-D114 | Use the existing crest-led non-video Academy hero, and hide the entire vertical-story section. Exclude both MP4s and both posters from this rollout. | Christian approved the recommended treatment on 2026-08-01. Bunny.net remains an approved but unimplemented capability outside this goal. |
| DCFC-D118 | Close Phase 4 at the local planning/verification boundary and explicitly defer every unsupplied hosted input. The Phase 4 manifest does not make `DCFC-501` eligible; staging inspection still needs exact safe inputs and a separate assignment/approval. | Christian explicitly scoped the unbudgeted goal to end after the planning packet and verification, before the first rollout package. This fail-closed lock avoids inventing hostnames, identities, billing identifiers, DNS ownership, launch dates, or observation duration. |

| DCFC-D112 | **Onzio purchases the club's domain and launches on it.** The production public site goes live on the club's own custom domain, not a `*.vercel.app` hostname. Onzio owns the registration and controls DNS. | Christian decided on 2026-08-03, and clarified the same day that the domain purchase in `DCFC-D119` means launching **on** that domain rather than deferring it — correcting a first pass that had recorded a `vercel.app` launch. Onzio controlling DNS removes the client-side DNS dependency that made the original `DCFC-D112` hard to answer. Consequences: `DCFC-902` (domain/DNS attachment) is **on the launch critical path**, not deferred; the purchase, DNS propagation, Vercel domain verification, the production Auth redirect allowlist, and the tenant's verified primary `club_domains` row all precede `DCFC-903`. Still required to execute: the exact domain name and whether apex or `www` is canonical — tracked as `DCFC-D123`. |
| DCFC-D113 | Production provisions **exactly one owner and no admins**. Additional admins are added by the owner through the `PLAT-101` owner-managed flow after launch. | Christian decided on 2026-08-03. Keeps `DCFC-803` to a single account and exercises the owner-managed membership path that `PLAT-101` shipped, rather than pre-provisioning through operator tooling. Identity values remain outside Git per this decision's original constraint; only the shape is recorded here. |
| DCFC-D116 | Observation window is **7 days**. Rollback authority is Christian, sole. Stop conditions: public site down, incorrect billing state, or any cross-tenant data exposure. | Christian decided on 2026-08-03, choosing the shorter end of the 7–14 day planning range. One low-traffic site; the second week adds little new signal. |
| DCFC-D117 | The production site retains `noindex, nofollow` through launch. Indexing remains a separate later approval after observation closes. | Christian confirmed on 2026-08-03. Unchanged from the planned shape; `DCFC-1003` still carries the indexing approval. |
| DCFC-D119 | Diverse City's price is **$75/month**, not $65. Onzio purchases and manages the club's domain; the $10 increase covers it. | Christian decided on 2026-08-03. Supersedes the $65/month figure in `PLAT-D008`, which is marked superseded in `docs/phase-12/DECISIONS.md`. Two consequences are recorded rather than assumed: (1) no live Stripe Price at $75/month exists — the recorded $65/month Starter Price can no longer be reused — so one must be created before `DCFC-901`, and both `PLAT-102` and the original `DCFC-D115` prohibit creating live Prices inside a package, leaving this an unowned manual step; (2) the domain is delivered at launch per `DCFC-D112`, so the club receives what the $75 covers from the first invoice and no billing-versus-delivery gap exists. Domain ownership on termination remains unpinned — see `DCFC-D122`. |

| DCFC-D120 | The live Stripe **Product and Price exist**: `Diverse City FC Pro Plan`, Product `prod_UwUmEgeunaSPSI`, **$75.00 USD per month, live mode, no trial, zero active subscriptions**. Created manually by Christian outside the packages, which is the path `DCFC-D119` anticipated. | Christian created and confirmed it on 2026-08-03. Confirms `DCFC-D119`'s $75 and `PLAT-D008`'s no-trial provision, and the Product description records that the domain is included, matching `DCFC-D112`. **No subscription exists and MRR is $0**, so `DCFC-901` remains the owner-driven Checkout step exactly as specified and nothing is being billed. Three follow-ups are open rather than assumed: the exact Price ID (`DCFC-D126`), a matching test-mode Price for `PLAT-102` rehearsal (`DCFC-D125`), and removal of tier language from the customer-facing name and description (`DCFC-D124`). This was a live-mode Stripe mutation performed by Christian directly, not by any agent or package. |

| DCFC-D125 | A matching **test-mode** `Diverse City FC Pro Plan` Product and $75/month Price exists, satisfying `PLAT-102`'s required test-mode Price input. | Christian created it on 2026-08-03 after the live Product, once it was noted that `PLAT-102`'s Stripe rehearsal is test-mode only and cannot use the live Price. The Price ID is still outstanding — see `DCFC-D126`. The tier-language problem in `DCFC-D124` applies to this Product too. |

| DCFC-D126 | Both Stripe Price IDs are recorded. **Live: `price_1TwbmvK6WajTkwHYueLvjhv5`** — the value `clubs.stripe_price_id` takes at `DCFC-901`. **Test: `price_1U0Y0sK6WajTkwHYnnttR9nN`** — for `PLAT-102`'s test-mode rehearsal. | Christian supplied both on 2026-08-03. Both are $75.00 USD per month with no trial, on the `Diverse City FC Pro Plan` Products (live `prod_UwUmEgeunaSPSI`). Both carry the same account infix as the already-recorded staging webhook `we_1TxrnaK6WajTkwHYtFEvCEo8`, confirming one Stripe account across environments. These are non-secret identifiers of the class this repository already records; no key or signing secret is stored. `PLAT-102` must read the **test** Price and never the live one, per its own prohibition on live-mode Stripe actions. |

| DCFC-D124 | Tier language is removed from **both** Stripe Products. Name is `Onzio - Diverse City FC` (plain hyphen, as applied); description is the tier-free wording pinned below. Applied to live and test on 2026-08-03. | The original `Diverse City FC Pro Plan` name and its "Everything in Starter, plus…" description described the tier ladder `PLAT-102` deletes, on artifacts customers read on invoices, receipts, and in the Customer Portal. The `Onzio - <Club>` name shape scales to every future club, which matters because `PLAT-D003` gives each club its own Product and negotiated Price. Three claims were dropped deliberately: **analytics** and **advanced content controls**, neither of which maps to a customer-facing feature in the codebase, and **expanded profiles**, which reads as a tier upgrade. Every remaining item maps to a real table. Applied in both modes so `PLAT-102`'s test rehearsal exercises the shipped wording. |

| DCFC-D127 | **Supersedes the `DCFC-D124` pinned description.** Live Stripe Products carry one shared, club-neutral, tier-free description, pinned below. Applied to `prod_UwUmEgeunaSPSI` and `prod_Uw0SrC4bw23myw` on 2026-08-06. `DCFC-D124`'s prohibition on tier language stands unchanged; only the wording is replaced. | Christian decided this on 2026-08-06 after `DCFC-701`. Two drivers. First, `PLAT-102` deletes tier gating, so Starter and Pro no longer differ by feature — only by price. Any description implying a feature ladder is now factually false, which is why the shared wording drops the club name rather than differentiating the products. Second, an agent had overwritten the `DCFC-D124` pinned text earlier that day with the exact "Everything in Starter, plus…" wording `DCFC-D124` deleted, while renaming Products toward tier language, because it proposed a per-tier convention without reading this file. That change is reverted by this decision. The naming question is deliberately left open — see `DCFC-D128`. `D124`'s discipline is retained: every item in the wording maps to a real table. |

| DCFC-D128 | **Two shared live Stripe Products, current names retained.** `Onzio Starter Plan` (`prod_Uw0SrC4bw23myw`, $65) and `Onzio Pro Plan` (`prod_UwUmEgeunaSPSI`, $75). Every club points `clubs.stripe_price_id` at one of these two Prices. **Supersedes `DCFC-D124`'s `Onzio - <Club>` naming.** `prod_V0ZBwEyJqipBOw` and `prod_Uw0TYfkWstKVTG` stay archived. | Christian decided this on 2026-08-06. An agent initially argued per-club Products were already mandated by `PLAT-D003`; that was wrong. `PLAT-D003` states only that negotiated prices live in `clubs.stripe_price_id` rather than environment configuration — it says nothing about Product shape, and two shared Products satisfy it because `clubs.stripe_price_id` simply names one of the two Prices. The per-club reading came from `DCFC-D124`'s rationale, not from `PLAT-D003` itself. Two shared Products are simpler and avoid Product proliferation per club. **Known caveat, accepted:** `PLAT-102` deletes tier gating, so the two Products are functionally identical and differ only in price; the names `Starter` and `Pro` therefore imply a feature difference that no longer exists. Christian accepted this as a price-point label. If it becomes a customer-comprehension problem, the fix is a single Product carrying both Prices — note that Prices cannot move between Products, so Rose City's `price_1TwbmvK6WajTkwHYueLvjhv5` would have to remain where it is. |

| DCFC-D129 | **Diverse City FC's live price is $85/month on a dedicated Product, not `DCFC-D126`'s shared $75 Price.** New Product `Onzio Diverse City FC Plan` (`prod_V34NKDxtbguIn0`), Price `price_1U2yF0K6WajTkwHYjgvrqAly`, $85.00 USD/month, no trial. `clubs.stripe_price_id` for `diverse-city` (club `d7a41762-5158-496e-b415-c83c01ab5c70`) was set to this Price on 2026-08-10 via `scripts/set-diverse-city-live-price.ts`, recorded in `audit_events` as `manual.diverse_city_live_price_set`. **Supersedes `DCFC-D126`'s live Price value for Diverse City specifically**, and is a one-club exception to `DCFC-D128`'s two-shared-Products policy — `D128`'s general policy is unchanged for every other club. | Christian negotiated $85/month directly with the client and deliberately created a per-club Product rather than reusing the shared Pro Price, since the price may change later if the client adds more scope. Discovered because `/api/stripe/checkout` was returning `STRIPE_PRICE_REQUIRED`: `clubs.stripe_price_id` had never been set at all (`DCFC-D126`'s $75 Price was recorded as the intended value but never written to the row), and by the time it was being fixed the actual agreed price had moved to $85 on a new Product. The `DCFC-D125`/`D126` test-mode Price (`price_1U0Y0sK6WajTkwHYnnttR9nN`) is untouched and still governs `PLAT-102`'s test-mode rehearsal. |

| DCFC-D129 | **Unconditional `noindex, nofollow` ships to production including Rose City's live site.** `middleware.ts` emits the header for every tenant regardless of `public_access` (commit `c906242`, implementing `DCFC-D117`). Rose City is currently `public_access=live` in production, so the release de-indexes a live customer site. Accepted. | Christian decided this on 2026-08-07 when it was surfaced during `DCFC-801` release planning. Rose City is `canceled`, paid through 2026-08-24, and is not being used going forward, so loss of search indexing carries no business cost. The alternative — scoping the header away from Rose City — is a code change that would reintroduce a `public_access`-shaped branch, which is exactly the `DCFC-D117` violation corrected earlier that day. Recorded because the effect is customer-visible and was not an intended consequence of the `DCFC-D117` fix; a future tenant that must stay indexed needs the explicit per-club opt-in described in `DCFC-D117`, not a special case here. |

| DCFC-D131 | **Supersedes `DCFC-D114`'s video disposition.** Christian chose to build the real Bunny.net Stream pipeline per `DCFC-D105` rather than ship the mockup's static poster stills as an interim substitute. Both approved videos (`homepage-hero-edited.mp4`, `club-reel-portrait.mp4`) are uploaded to Bunny Stream library `723074` ("onzio"), transcoded, and live on production's `academy@1` homepage: the hero (`components/Hero.tsx`'s new `academy@1` branch) and the new "Developing the Next Generation" story section (`components/DevelopingNextGeneration.tsx`). `DCFC-D114`'s crest-only hero and hidden story section no longer apply to `academy@1`; the "hide entire vertical-story section" and "exclude both MP4s and posters" clauses are superseded for this club. | Christian said "Yes, lets use another session to do this" when offered the choice between the real pipeline and the poster-image interim, on 2026-08-07, choosing the real pipeline. Implemented via Bunny Stream's HTTP API (library-scoped API key as the `AccessKey` header against `video.bunnycdn.com/library/723074/...`, confirmed by Bunny's own docs stating every Stream HTTP API request is authenticated with the per-library key, not the account key). Delivery uses Bunny's per-resolution MP4 fallback (`play_720p.mp4`) as the native `<video>` source rather than HLS + `hls.js`, avoiding a new dependency while still working in every modern browser; both videos finished transcoding (status 4/"Finished") within about 30 seconds of upload. Video GUIDs and the CDN pull-zone hostname are recorded in `lib/bunny-video.ts` as non-secret identifiers; the Bunny API keys themselves are read from `.env.local` at request time and never committed. Both sections fall back to the same real, already-approved poster stills (`public/images/video/diverse-city-hero-poster.jpg`, `diverse-city-club-reel-poster.jpg`) as static images if Bunny playback ever fails, via a new `ResilientBunnyVideo` component following this codebase's existing `ResilientNativeImage` convention. Both videos are currently Diverse-City-specific; a future `academy@1` club would need its own videos and GUIDs rather than reusing these. |
| DCFC-D132 | **Supersedes `DCFC-D104`'s palette stance only.** Christian verbally overrode `DCFC-D104` during a live session on 2026-08-07: `academy@1` should match the sales mockup's exact palette (ground `#F9FAFD`, ink/dark bands navy `#1E3653`/`#14283F`, accent red `#FF1616` with hover `#D70000`, sky accent `#B9E3F6`, muted `#6B7E94`/`#EDF2F7`), not maintain its own distinct near-black one. `DCFC-D104`'s template-registration decision itself (academy@1 as its own neutral reusable template) is unchanged — only the "own palette, not a mockup clone" reading is superseded. | Implemented 2026-08-07 by making the Tailwind brand aliases and `--color-*` variables CSS-variable-driven and overriding them under the existing `[data-font-pack="academy"]` scope (plus `body:has(...)` for the body element itself), so every other template resolves to byte-identical colors as before. Applied together with the mockup-parity component pass (footer, hero, nav, sponsor band, programs, shop, contact, tryouts) recorded in `STATUS.md`'s 2026-08-07 mockup-parity entry. |
| DCFC-D130 | **No dedicated `/sponsors` page for `academy@1`.** `app/(public)/sponsors/page.tsx` renders a real listing only for `clubhouse@1` (Rose City); every other template, including Diverse City's `academy@1`, falls through to a static "Partners are not published for this site yet." stub regardless of actual `onzio.site_sponsor_logos` data. Accepted as-is for Diverse City, not deferred. | Surfaced during the 2026-08-07 pixel-perfect mockup-vs-production sweep: Diverse City has 2 real sponsor-logo rows (`carousel`, `footer` placements — Elsa's Bakery, visible in every page footer), so the stub's "not published" message doesn't reflect actual data. Christian's exact words: "Since the sponsor page didnt exist, lets not add it." No `academy@1` sponsors page has ever existed, so this closes the gap as intentional rather than leaving it as an open backlog item. Revisit only if Diverse City (or a future `academy@1` club) accumulates enough partners to justify a dedicated listing page beyond the carousel/footer placements. |
| DCFC-D133 | **Supersedes `DCFC-D113`'s owner-count clause only.** Production keeps **two permanent owners**, not one: Christian (the `DCFC-801` provisioning bootstrap identity) stays an owner alongside the real club owner invited under `DCFC-803` on 2026-08-11. `DCFC-D113`'s other clause — no admins pre-provisioned, added by the owner through the `PLAT-101` self-service flow after launch — is unchanged. | Christian decided this on 2026-08-11 after asking what removing his own owner membership would do, and being walked through the actual mechanics before deciding: `club_members` access is scoped per club and entirely separate from operator privileges (`ONZIO_OPERATOR_USER_IDS`), and `removeClubMembership`'s `LAST_OWNER_REQUIRED` guard already treats more than one active owner as a normal, supported state rather than a workaround — nothing technical forced the original one-owner design. Two consequences recorded rather than assumed: (1) both owners can reach `/admin/payments` and the Stripe checkout/portal routes, which gate only on `role === 'owner'` with no further distinction between multiple owners — Onzio retaining billing-management access to this customer's account going forward is accepted, not an oversight; (2) the self-service Team Access page (`/admin/members`) only lists `role = 'admin'` rows (`listClubAdmins`), so neither owner is shown to the other there — if the club owner should know Christian retains access, that has to be said directly, since the product does not surface it. Identity values remain outside Git per `DCFC-D113`'s original constraint; only the shape is recorded here. |

**Pinned description for `DCFC-D127`.** Live Stripe Products carry this text;
changes need a new decision. Supersedes the `DCFC-D124` wording.

> Hosted club website managed through Onzio. Includes roster, schedule,
> standings, seasons, programs, tryouts, store, sponsors, and contact pages,
> with media management and admin access for club staff. Custom domain
> registration and management included.

**Test-mode parity for `DCFC-D127` is outstanding.** The test-mode Products
(`prod_V1iYXl47nqE0hM` at $65, `prod_V0ZAFI3eQv4qxZ` at $75) still carry their
previous descriptions. `DCFC-D124` required both modes to match so the
`PLAT-102` rehearsal exercises shipped wording; that requirement carries
forward. This must be applied by Christian, since agent Stripe access is
live-mode only.

## Open Decisions

| ID | Decision needed | Owner | Required before | Resolution evidence |
| --- | --- | --- | --- | --- |
The five rollout decisions carried since `DCFC-D118` were answered by Christian
on 2026-08-03 and moved to Accepted above as `DCFC-D112`, `DCFC-D113`,
`DCFC-D116`, `DCFC-D117`, and `DCFC-D119`. `DCFC-D115` is partly resolved —
the price is settled by `DCFC-D119` — and its unresolved remainder is split out
below rather than closed.

| ID | Decision needed | Owner | Required before | Resolution evidence |
| --- | --- | --- | --- | --- |
| DCFC-D107 | Whether a future Contact submission form is needed | Christian/client | Outside this epic | Separate scoped epic if approved |
| DCFC-D123 | Exact domain to purchase, and whether apex or `www` is canonical. Onzio buys and controls DNS per `DCFC-D112`, so only the name and canonical form remain. | Christian/client | Before `DCFC-902`; the purchase itself should start now because DNS and verification are calendar time, not work | Recorded hostname manifest with no secret values |
| DCFC-D122 | Who owns the purchased domain if the club leaves Onzio. Not a technical blocker, but unpinned commercial terms around a domain Onzio buys on a customer's behalf. | Christian/client | Before or at `DCFC-901` | Recorded commercial term |
| DCFC-D115 | ~~Live billing activation inputs~~ — price resolved by `DCFC-D119`; remainder split into `DCFC-D120`, `DCFC-D121`, and `DCFC-D122`. Billing owner/contact and owner-driven Checkout timing still confirm at `DCFC-901`. | Christian/client | Reverified before `DCFC-901` | Dated commercial approval plus safe live Price identifier |

## Decision Rules

- Agents may document options and evidence but may not silently resolve an open
  product or architecture decision.
- A decision that changes platform architecture must also update
  `docs/onzio-platform-plan.md` after Christian approves it.
- When a decision is accepted or superseded, record the date, approver, and
  evidence in this file and update affected work packages and `STATUS.md`.
